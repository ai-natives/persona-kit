{
  "name": "personakit-admin-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "./start.sh",
    "stop": "./stop.sh",
    "install": "cd frontend && pnpm install",
    "build": "cd frontend && pnpm run build",
    "preview": "cd frontend && pnpm run preview"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}